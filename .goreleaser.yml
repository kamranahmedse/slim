version: 2

builds:
  - main: .
    binary: localname
    ldflags:
      - -s -w -X github.com/kamranahmedse/localname/cmd.Version={{.Version}}
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: "localname_{{.Version}}_{{.Os}}_{{.Arch}}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc

release:
  github:
    owner: kamranahmedse
    name: localname
